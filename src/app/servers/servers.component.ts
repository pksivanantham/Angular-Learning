import { Component } from "@angular/core";

@Component({
    //selector:'[app-servers]', /* By attribute selector*/
    //selector:'.app-servers', /* By class selector*/
    selector:'app-servers',
    templateUrl:'./servers.component.html',
    styleUrls:['./servers.component.scss']//Style urls is an array because we can provide n number of css
})
export class ServersComponent {

}