import { Component } from "@angular/core";

@Component({
    selector:'app-server',
    templateUrl:'./server.component.html',
    styleUrls:['./server.component.scss']//Style urls is an array because we can provide n number of css
})
export class ServerComponent {

}